// import { selectBarChartDomain } from '../selectors';
import expect from 'expect';

// const selector = selectBarChartDomain();

describe('selectBarChartDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
