// import { selectResponsiveChartAreaDomain } from '../selectors';
import expect from 'expect';

// const selector = selectResponsiveChartAreaDomain();

describe('selectResponsiveChartAreaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
